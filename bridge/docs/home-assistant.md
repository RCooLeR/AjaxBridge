# Home Assistant

AjaxBridge integrates with Home Assistant through MQTT Discovery. Home Assistant creates devices and entities from retained discovery topics, while AjaxBridge publishes retained JSON state topics for each account, SIA zone, and optional Jeedom device.

External references:

- Home Assistant installation: https://www.home-assistant.io/installation/
- Home Assistant MQTT integration and discovery: https://www.home-assistant.io/integrations/mqtt

## Overview And How It Works

AjaxBridge publishes:

- an availability topic
- SIA account state topics
- SIA zone state topics
- optional Jeedom device state topics
- retained Home Assistant MQTT discovery configs
- optional Home Assistant switch discovery for allowlisted Jeedom controls

Home Assistant reads the discovery configs under the discovery prefix, default `homeassistant`, and creates native MQTT entities. Every entity for one Ajax account or device points at one JSON state topic and uses a template to read one field.

SIA entities and Jeedom entities can belong to the same Home Assistant device. This is how the bridge prevents duplicate devices when both sources describe the same physical Ajax device.

## Install And Configure Home Assistant

1. Install Home Assistant using the official installation guide.
2. Install or choose an MQTT broker.
   - Home Assistant OS users commonly install the Mosquitto Broker add-on.
   - Existing Mosquitto or another MQTT broker is also fine.
3. Add the MQTT integration in Home Assistant under `Settings -> Devices & services`.
4. Confirm MQTT Discovery is enabled. The default discovery prefix is `homeassistant`.
5. Configure AjaxBridge to connect to the same broker.

Bridge MQTT environment:

```yaml
environment:
  AJAXBRIDGE_MQTT_BROKER: "tcp://homeassistant.local:1883"
  AJAXBRIDGE_MQTT_USERNAME: "mqtt-user"
  AJAXBRIDGE_MQTT_PASSWORD: "mqtt-password"
  AJAXBRIDGE_MQTT_CLIENT_ID: "ajaxbridge"
  AJAXBRIDGE_MQTT_TOPIC_PREFIX: "ajaxbridge"
  AJAXBRIDGE_MQTT_DISCOVERY: "true"
  AJAXBRIDGE_MQTT_DISCOVERY_PREFIX: "homeassistant"
  AJAXBRIDGE_MQTT_RETAIN: "true"
```

If the broker does not require authentication, only `AJAXBRIDGE_MQTT_BROKER` is required.

Common broker URLs:

```text
tcp://homeassistant.local:1883
tcp://HOME_ASSISTANT_IP:1883
tcp://mosquitto:1883
```

Use the hostname or IP address reachable from the AjaxBridge container.

Restart AjaxBridge after changing MQTT settings:

```bash
docker compose up -d
docker compose logs -f ajaxbridge
```

Expected log:

```text
MQTT connected
```

If MQTT is unavailable, SIA handling continues. Ajax ACK responses are not blocked by MQTT publishing.

## MQTT Topics

Default configuration:

| Setting | Default |
| --- | --- |
| MQTT state prefix | `ajaxbridge` |
| MQTT discovery prefix | `homeassistant` |
| discovery node | `ajaxbridge` |
| availability topic | `ajaxbridge/status` |

Availability:

```text
ajaxbridge/status = online | offline
```

SIA account state:

```text
ajaxbridge/accounts/<account>/state
```

SIA zone state:

```text
ajaxbridge/accounts/<account>/zones/<zone>/state
```

Jeedom state:

```text
ajaxbridge/jeedom/devices/<device_slug>/state
```

Discovery topic format:

```text
<discovery_prefix>/<component>/<node>/<object_id>/config
```

Example:

```text
homeassistant/binary_sensor/ajaxbridge/zone_a0f80d_8_alarm_active/config
```

Home Assistant entity ids are generated by Home Assistant and can change if the user renames entities or if collisions exist. For dashboards and cards, treat `unique_id`, MQTT object id, and JSON payload fields as stable. Let users configure final HA `entity_id` values.

## Device Creation And Naming

### SIA Account Device

One Home Assistant device is created per SIA account.

| Field | Value |
| --- | --- |
| HA identifier | `ajaxbridge_account_<account>` |
| Name | `Ajax account <account>` |
| Manufacturer | `Ajax Systems` |
| Model | `Ajax account` |

Example:

```json
{
  "identifiers": ["ajaxbridge_account_A0F80D"],
  "name": "Ajax account A0F80D",
  "manufacturer": "Ajax Systems",
  "model": "Ajax account"
}
```

### SIA Zone Device

One Home Assistant device is created per SIA account/zone.

| Field | Value |
| --- | --- |
| HA identifier | `ajaxbridge_<account>_zone_<zone>` |
| Name | Catalog `name`, fallback `Ajax zone <zone>` |
| Manufacturer | `Ajax Systems` |
| Model | Catalog `kind`, fallback `Ajax device` |
| Area | Catalog `room` when not empty and not `unknown` |

Example:

```json
{
  "identifiers": ["ajaxbridge_A0F80D_zone_8"],
  "name": "Server power",
  "manufacturer": "Ajax Systems",
  "model": "WallSwitch",
  "suggested_area": "Boiler room"
}
```

### Jeedom Linked Device

When a Jeedom device is linked by `jeedom_names` or `jeedom_command_ids`, its entities reuse the SIA zone identifier:

```json
{
  "identifiers": ["ajaxbridge_A0F80D_zone_8"],
  "name": "Server power",
  "manufacturer": "Ajax Systems",
  "model": "WallSwitch"
}
```

This is the duplicate-prevention rule. Home Assistant sees SIA alarm/trouble entities, Jeedom metrics, and optional Jeedom control entities as entities of the same physical device.

For linked devices, SIA-owned security/status values stay authoritative. Jeedom commands that duplicate SIA concepts such as tamper, bypass, external power, fire, smoke, water leak, and hardware/connectivity trouble are cleaned from Home Assistant discovery and are not rediscovered. The normalized base alarms `smoke_alarm`, `heat_alarm`, and `carbon_monoxide_alarm` follow that rule; richer Jeedom-only detail such as `rapid_temperature_rise_alarm`, `critical_smoke_alarm`, and `critical_carbon_monoxide_alarm` remains discoverable. Jeedom still publishes its normalized state JSON for debugging and notifications, and Home Assistant still gets Jeedom-only measurements such as `temperature_c`, `humidity_percent`, `co2_ppm`, `power_w`, `current_a`, `voltage_v`, and `energy_kwh`.

### Jeedom Unlinked Device

Unlinked Jeedom devices use:

```text
ajaxbridge_jeedom_<device_slug>
```

By default `AJAXBRIDGE_JEEDOM_DISCOVER_UNLINKED=false`, so unlinked Jeedom data is published to MQTT state and debug endpoints but does not create HA entities. Enable it only for real Jeedom-only devices.

### Jeedom Account/Hub Device

Use `AJAXBRIDGE_JEEDOM_ACCOUNT_NAMES` for Jeedom equipment that represents the Ajax hub/account:

```yaml
AJAXBRIDGE_JEEDOM_ACCOUNT_NAMES: "House"
```

Those entities attach to:

```text
ajaxbridge_account_<account>
```

## Device Catalog

Catalog path:

```text
data/devices.json
```

Fields:

| Field | Purpose |
| --- | --- |
| `account` | SIA account/object number. Required for stable HA identity. |
| `zone` | SIA zone. Required for stable HA identity. |
| `partition` | Optional SIA partition/area. |
| `group` | Optional group. |
| `device` | Optional Ajax device id for exact lookup. |
| `name` | HA device name and dashboard title. |
| `room` | HA suggested area and dashboard grouping. |
| `kind` | HA model and dashboard category. |
| `events` | Stable signal list used to pre-create binary sensors. |
| `jeedom_names` | Jeedom aliases that map to this SIA zone. |
| `jeedom_command_ids` | Jeedom command ids that map to this SIA zone. |
| `description` | Human note. |

Example:

```json
[
  {
    "account": "A0F80D",
    "zone": "8",
    "name": "Server power",
    "room": "Boiler room",
    "kind": "WallSwitch",
    "events": ["power", "connectivity", "hardware", "firmware"],
    "jeedom_names": ["Server power", "Serverna"],
    "jeedom_command_ids": ["52", "53", "54", "55", "56", "57", "58", "59"]
  }
]
```

## Account Entities

Object id prefix:

```text
account_<account>_
```

Unique id:

```text
ajaxbridge_account_<account>_<suffix>
```

State topic:

```text
ajaxbridge/accounts/<account>/state
```

Payload:

```json
{
  "account": "A0F80D",
  "online": true,
  "mode": "disarmed",
  "armed": false,
  "night_mode": false,
  "partially_armed": false,
  "alarm_active": false,
  "tamper_active": false,
  "trouble_active": false,
  "last_event_code": "BR",
  "last_event_name": "Burglary alarm restored",
  "last_signal": "burglary",
  "last_event_at": "2026-05-05T10:30:00Z",
  "last_event_unix": 1777977000,
  "last_ping_at": "2026-05-05T10:31:00Z",
  "last_ping_unix": 1777977060
}
```

Entities:

| Component | Object suffix | Name | Value |
| --- | --- | --- | --- |
| `binary_sensor` | `online` | `Online` | `value_json.online` |
| `binary_sensor` | `armed` | `Armed` | `value_json.armed` |
| `binary_sensor` | `night_mode` | `Night mode` | `value_json.night_mode` |
| `binary_sensor` | `partially_armed` | `Partially armed` | `value_json.partially_armed` |
| `binary_sensor` | `alarm_active` | `Alarm active` | `value_json.alarm_active` |
| `binary_sensor` | `tamper_active` | `Tamper active` | `value_json.tamper_active` |
| `binary_sensor` | `trouble_active` | `Trouble active` | `value_json.trouble_active` |
| `sensor` | `mode` | `Mode` | `value_json.mode` |
| `sensor` | `last_event_name` | `Last event` | `value_json.last_event_name` |
| `sensor` | `last_event_code` | `Last event code` | `value_json.last_event_code` |
| `sensor` | `last_signal` | `Last signal` | `value_json.last_signal` |
| `sensor` | `last_event_at` | `Last event time` | `value_json.last_event_at` |
| `sensor` | `last_ping_at` | `Last ping time` | `value_json.last_ping_at` |

Account entities receive stable metadata from:

```text
ajaxbridge/accounts/<account>/attributes
```

The attributes payload contains only `account`. Dynamic state and timestamps remain available as dedicated entities and in the unchanged `/state` MQTT payload, so routine state messages do not force attribute updates for every account entity.

## Zone Entities

Object id prefix:

```text
zone_<account>_<zone>_
```

State topic:

```text
ajaxbridge/accounts/<account>/zones/<zone>/state
```

Payload:

```json
{
  "account": "A0F80D",
  "partition": "1",
  "group": "",
  "zone": "8",
  "device": "8",
  "device_name": "Server power",
  "room": "Boiler room",
  "kind": "WallSwitch",
  "device_events": ["power", "connectivity", "hardware", "firmware"],
  "alarm_active": false,
  "alarm_signal": "none",
  "alarm_action": "none",
  "alarm_event_code": "",
  "alarm_event_name": "",
  "tamper_active": false,
  "trouble_active": false,
  "signal_active": {
    "power": false,
    "connectivity": false,
    "hardware": false,
    "firmware": false
  },
  "last_event_code": "BR",
  "last_event_name": "Burglary alarm restored",
  "last_signal": "burglary",
  "last_event_at": "2026-05-05T10:30:00Z",
  "last_event_unix": 1777977000,
  "alarm_started_at": "",
  "alarm_started_unix": 0
}
```

Base entities for every zone:

| Component | Object suffix | Name | Value |
| --- | --- | --- | --- |
| `binary_sensor` | `alarm_active` | `Alarm active` | `value_json.alarm_active` |
| `binary_sensor` | `tamper_active` | `Tamper active` | `value_json.tamper_active` |
| `binary_sensor` | `trouble_active` | `Trouble active` | `value_json.trouble_active` |
| `sensor` | `last_event_name` | `Last event` | `value_json.last_event_name` |
| `sensor` | `last_event_code` | `Last event code` | `value_json.last_event_code` |
| `sensor` | `last_signal` | `Last signal` | `value_json.last_signal` |
| `sensor` | `last_event_at` | `Last event time` | `value_json.last_event_at` |
| `sensor` | `alarm_signal` | `Alarm signal` | `value_json.alarm_signal` |
| `sensor` | `alarm_action` | `Alarm action` | `value_json.alarm_action` |

Signal entities are created for every signal in `device_events` and every observed runtime signal:

```text
zone_<account>_<zone>_signal_<signal>
```

The SIA `power` and `temperature` signals use explicit alarm ids to stay separate from Jeedom measurement sensors:

```text
zone_<account>_<zone>_signal_power_failure
zone_<account>_<zone>_signal_temperature_alarm
```

Template:

```jinja
{{ 'ON' if value_json.signal_active.get('<signal>', false) else 'OFF' }}
```

Signal presentation:

| Signal | Name | Device class or icon |
| --- | --- | --- |
| `fire` | `Fire` | `safety` |
| `smoke` | `Smoke` | `smoke` |
| `water_leak` | `Water leak` | `moisture` |
| `battery` | `Battery low` | `battery` |
| `tamper` | `Tamper` | `tamper` |
| `connectivity` | `Connection lost` | `problem` |
| `hardware` | `Hardware trouble` | `problem` |
| `firmware` | `Firmware trouble` | `mdi:update` |
| `power` | `Power failure` | `mdi:power-plug-off` |
| `burglary` | `Burglary` | `mdi:shield-alert` |
| `bypass` | `Bypassed` | `problem` |
| `tamper_bypass` | `Tamper bypass` | `problem` |
| `interference` | `Interference` | `problem` |
| `accelerometer` | `Accelerometer` | `problem` |
| `fire_detector` | `Fire detector trouble` | `problem` |
| `duress` | `Duress` | `safety` |
| `emergency` | `Emergency` | `safety` |
| `medical` | `Medical` | `safety` |
| `panic` | `Panic` | `safety` |
| `temperature` | `Temperature alarm` | `safety` |
| `arming` | `Arming` | `mdi:shield-lock` |
| `night_mode` | `Night mode` | `mdi:weather-night` |

Zone entities receive stable metadata from:

```text
ajaxbridge/accounts/<account>/zones/<zone>/attributes
```

The compact attributes payload contains `account`, `partition`, `group`, `zone`, `device`, `device_name`, `room`, `kind`, and `device_events`. Alarm state, active signals, and timestamps remain in the unchanged `/state` payload and their dedicated Home Assistant entities.

## Jeedom Entities

Numeric temperature, power, current, voltage, energy, and similar actual values come from Jeedom, not from SIA. SIA `temperature` and `power` entries are binary alarm/trouble signals and are discovered as separate `signal_temperature_alarm` and `signal_power_failure` binary sensors.

Jeedom discovery topic:

```text
homeassistant/<component>/ajaxbridge/jeedom_cmd_<command_id>/config
```

Relay `voltage_v` values are normalized by AjaxBridge before publishing. The Jeedom Ajax plugin reports relay supply voltages in tenths of volts, so a raw value such as `289.02` is exposed to Home Assistant as `28.902`.

Unique id:

```text
ajaxbridge_jeedom_cmd_<command_id>
```

State topic:

```text
ajaxbridge/jeedom/devices/<device_slug>/state
```

Stable attributes topic:

```text
ajaxbridge/jeedom/devices/<device_slug>/attributes
```

Value templates:

```jinja
{{ value_json.<metric> }}
{{ 'ON' if value_json.<metric> else 'OFF' }}
```

Known Jeedom entities:

| Component | Metric | Name | Unit | Device class | State class |
| --- | --- | --- | --- | --- | --- |
| `sensor` | `power_w` | `Power` | `W` | `power` | `measurement` |
| `sensor` | `energy_kwh` | `Energy` | `kWh` | `energy` | `total_increasing` |
| `sensor` | `current_a` | `Current` | `A` | `current` | `measurement` |
| `sensor` | `voltage_v` | `Voltage` | `V` | `voltage` | `measurement` |
| `sensor` | `temperature_c` | `Temperature` | `C` | `temperature` | `measurement` |
| `sensor` | `battery_percent` | `Battery` | `%` | `battery` | `measurement` |
| `sensor` | `battery_state` | `Battery state` | none | none | none |
| `sensor` | `signal_dbm` | `Signal` | `dBm` | `signal_strength` | `measurement` |
| `sensor` | `signal_level` | `Signal` | none | none | none |
| `sensor` | `humidity_percent` | `Humidity` | `%` | `humidity` | `measurement` |
| `binary_sensor` or `sensor` | `state` | `State` | none | none | none |
| `binary_sensor` | `online` | `Online` | none | `connectivity` | none |
| `binary_sensor` | `tamper` | `Tamper` | none | `tamper` | none |
| `binary_sensor` | `external_power` | `External power` | none | `power` | none |
| `binary_sensor` | `cellular_data_active` | `Cellular data active` | none | `connectivity` | none |
| `sensor` | `gsm_network_type` | `GSM network type` | none | none | none |
| `binary_sensor` | `opening` | `Opening` | none | `opening` | none |
| `binary_sensor` | `door` | `Door` | none | `door` | none |
| `binary_sensor` | `leak` | `Leak` | none | `moisture` | none |

These metrics are kept in state/debug JSON but are intentionally not Home Assistant discovered to avoid noisy entities:

- `event_source`
- `event`
- `event_code`

## Jeedom Controls

Controls are created only when:

- `AJAXBRIDGE_JEEDOM_CONTROLS_ENABLED=true`
- eqLogic discovery was received
- the device type is allowlisted

Control types:

- `Socket`, `WallSwitch`, `LightSwitch`, `Outlet`, and `WaterStop` publish Home Assistant MQTT switches when both `on` and `off` actions exist.
- `Relay` publishes a Home Assistant MQTT button for impulse control. AjaxBridge uses a discovered `impulse` action when available, otherwise it uses the relay `on` action as the pulse.

Blocked:

- hub/security controls
- any unknown device type

Home Assistant switch discovery:

```text
homeassistant/switch/ajaxbridge/jeedom_control_<device_slug>/config
```

Home Assistant relay impulse button discovery:

```text
homeassistant/button/ajaxbridge/jeedom_control_<device_slug>_impulse/config
```

Unique id:

```text
ajaxbridge_jeedom_control_<device_slug>
```

Command topic:

```text
ajaxbridge/jeedom/devices/<device_slug>/set
```

Payloads:

```text
ON
OFF
```

The switch keeps the same unique id and command topic whether or not Jeedom exposes an `Etat`/`realState` info command. AjaxBridge always publishes a retained bridge state topic for the control. For action-only WallSwitch equipment it persists successful ON/OFF requests and recognized Ajax event-code changes, then republishes that state after restart. Positive power/current can seed `ON` during a fresh synchronization; zero load is not sufficient evidence for `OFF`. Until AjaxBridge has reliable evidence, Home Assistant shows the switch as `unknown` instead of incorrectly showing `OFF`.

Relay impulse buttons publish `ON` for an `on`-backed pulse or `IMPULSE` for a dedicated impulse action.

AjaxBridge then publishes to Jeedom:

```text
jeedom/cmd/set/<command_id>
```

The Jeedom action payload is `1` by default. Override `AJAXBRIDGE_JEEDOM_CONTROL_PAYLOAD` if your MQTT Manager command expects another payload.

Every HTTP or MQTT control attempt is visible at:

```text
GET /jeedom/control-audit?limit=100
```

The same Jeedom state and control data can drive AjaxBridge notifications. A `state` metric change can notify when a WallSwitch or Outlet turns on/off, and `control_on`, `control_off`, or `control` can notify when Home Assistant sends a command through AjaxBridge.

## Dashboard And Card Guide

Recommended card anchors:

| Card type | Anchor entity |
| --- | --- |
| Account summary | `binary_sensor.account_<account>_online` |
| Zone security tile | `binary_sensor.zone_<account>_<zone>_alarm_active` |
| Device trouble tile | `binary_sensor.zone_<account>_<zone>_trouble_active` |
| Jeedom power tile | A linked Jeedom `power_w`, `current_a`, or `voltage_v` entity |
| Controllable outlet/switch | `switch.jeedom_control_<device_slug>` or the final HA entity id |
| Relay impulse | `button.jeedom_control_<device_slug>_impulse` or the final HA entity id |

Because Home Assistant entity ids can be renamed, custom cards should let the user configure anchor entity ids. Read dynamic values from their dedicated entities. Compact attributes remain available for stable device classification (`kind`, `device_slug`, and `jeedom_device_type`); use the AjaxBridge HTTP API or MQTT `/state` topic for full debug data.

Stable keys:

| Context | Stable key |
| --- | --- |
| Account | `account` |
| SIA zone | `account + zone` |
| SIA HA device | `ajaxbridge_<account>_zone_<zone>` |
| Jeedom command | `command_id` |
| Jeedom metric | `metric` |
| Jeedom linked device | linked `account + zone` |
| Jeedom action | `device_slug + action` |

Recommended status priority for a device tile:

1. `alarm_active`
2. `tamper_active`
3. `trouble_active`
4. active values in `signal_active`
5. `online=false`
6. normal

For linked Jeedom metrics, show power/current/voltage/energy/temperature as secondary details. Do not let Jeedom `state` override SIA alarm, tamper, or trouble visuals.

Useful dashboard groupings:

| Device type | Primary SIA fields | Secondary Jeedom fields | Optional command |
| --- | --- | --- | --- |
| Relay | `alarm_active`, `trouble_active`, `signal_active.power` | `power_w`, `current_a`, `voltage_v`, `energy_kwh`, `online` | `button.jeedom_control_<slug>_impulse` |
| Socket/wall switch/outlet | `alarm_active`, `trouble_active`, `signal_active.power` | `power_w`, `current_a`, `voltage_v`, `energy_kwh`, `online`, `state` | `switch.jeedom_control_<slug>` |
| Leak detector | `signal_active.water_leak`, `tamper_active`, `trouble_active` | `battery_percent`, `online`, `signal_level` | none |
| Fire detector | `signal_active.fire`, `signal_active.smoke`, `tamper_active`, `trouble_active` | `temperature_c`, `battery_percent`, `online` | none |
| Door/window sensor | `signal_active.burglary`, `tamper_active`, `trouble_active` | `opening`, `battery_percent`, `online` | none |
| Hub/account | `mode`, `armed`, `night_mode`, `alarm_active`, `trouble_active` | GSM/CMS/Ethernet diagnostics if linked through `AJAXBRIDGE_JEEDOM_ACCOUNT_NAMES` | none |

## Verify MQTT Manually

State topics:

```bash
mosquitto_sub -h HOME_ASSISTANT_IP -u mqtt-user -P mqtt-password -t "ajaxbridge/#" -v
```

Discovery topics:

```bash
mosquitto_sub -h HOME_ASSISTANT_IP -u mqtt-user -P mqtt-password -t "homeassistant/#" -v
```

With MQTT Explorer, inspect:

```text
ajaxbridge/
ajaxbridge/jeedom/
homeassistant/binary_sensor/ajaxbridge/
homeassistant/sensor/ajaxbridge/
homeassistant/switch/ajaxbridge/
```

## Troubleshooting

No devices appear:

- Confirm Home Assistant MQTT integration is configured.
- Confirm MQTT discovery is enabled and uses prefix `homeassistant`.
- Confirm AjaxBridge logs `MQTT connected`.
- Subscribe to `homeassistant/#` and confirm retained discovery configs exist.
- Subscribe to `ajaxbridge/#` and confirm state topics exist.

Duplicate devices:

- Add `jeedom_names` and `jeedom_command_ids` to `data/devices.json`.
- Keep `AJAXBRIDGE_JEEDOM_DISCOVER_UNLINKED=false`.
- Set `AJAXBRIDGE_JEEDOM_ACCOUNT_NAMES` for hub/system Jeedom equipment.
- Do not delete SIA signal entities just to solve Jeedom duplication. Linked Jeedom commands that duplicate SIA security/status values are cleaned automatically; the SIA entity remains authoritative.
- AjaxBridge also publishes retained cleanup for old SIA discovery object id formats, including old `ajax2prometheus` topics, no-account zone ids such as `zone_13_alarm_signal`, and old signal ids such as `zone_a0f80d_13_firmware`.
- Devices shown as manufacturer `Ajax via Jeedom` and model `Jeedom MQTT Bridge` are unlinked Jeedom discovery entries. If the same physical device also exists as `Ajax Systems`, correct command ownership and allow the bridge to reconcile its discovery.
- Restart AjaxBridge. If `AJAXBRIDGE_JEEDOM_STORE_PATH` points at persisted storage such as `/data/jeedom.json`, AjaxBridge reloads the last Jeedom command cache and republishes linked discovery/state automatically. Force Jeedom MQTT Manager to republish eqLogic discovery only if the cache is empty or stale.
- Compare the Home Assistant entity's `unique_id` with retained discovery before removing anything. A suffix such as `_2` does not by itself prove a duplicate. If discovery identifies the correct device but the registry still points to an old owner, back up the registry and reload the MQTT integration to reconcile it while preserving entity identity.

Inspect retained discovery with Mosquitto clients:

```bash
BROKER=mqtt.example.test

mosquitto_sub -h "$BROKER" -t 'homeassistant/+/ajaxbridge/+/config' -W 3 -v
```

If the broker requires credentials, use the same MQTT credentials configured for your deployment. Remove an exact retained topic manually only after identifying it as obsolete and backing up its payload. Clearing every discovery or state topic can remove active entities and their registry settings; it is not a repair for electrical units or statistics. See [electrical history guidance](jeedom.md#existing-home-assistant-electrical-history).

Wrong names or rooms:

- Edit `data/devices.json`.
- Keep `account` and `zone` stable.
- Restart AjaxBridge so discovery configs are republished.

Jeedom control entity missing:

- Confirm `AJAXBRIDGE_JEEDOM_CONTROLS_ENABLED=true`.
- For Socket, WallSwitch, LightSwitch, and Outlet toggles, confirm `/jeedom/actions` has both `on` and `off` with `allowed: true`.
- For Relay impulse buttons, confirm `/jeedom/actions` has `impulse` or `on` with `allowed: true`.
- Confirm the device type is `Relay`, `Socket`, `WallSwitch`, `LightSwitch`, or `Outlet`.
- Confirm eqLogic discovery was received from Jeedom MQTT Manager.
- Confirm MQTT Manager accepts `jeedom/cmd/set/<command_id>` with payload `1`, or configure `AJAXBRIDGE_JEEDOM_CONTROL_PAYLOAD`.

Numeric Jeedom values missing:

- Compare `/jeedom/devices` and `/jeedom/commands` for the expected device. Electrical metric names follow verified units, for example `current_ma`, `current_a`, `energy_wh`, or `energy_kwh`. Unverified readings remain `current_raw` / `power_raw`; unknown readings are not replaced with zero.
- Confirm `/jeedom/commands` contains the matching Jeedom command ids and that those ids are listed in `data/devices.json` under `jeedom_command_ids`.
- If `data/jeedom.json` is empty or stale, force Jeedom MQTT Manager to republish eqLogic discovery and command events after changing mappings.
